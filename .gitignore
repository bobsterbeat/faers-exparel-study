# Raw FAERS data — 5.7 GB, public domain, fetched by download_faers.sh
data/raw/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Credentials
.env
.env.*
!.env.example

# Large intermediate files (>100 MB) — regenerable from scripts
# Keep the smaller outputs; exclude anything GitHub won't accept.
out/analysis_dataset.parquet
out/case_meta.parquet

# Pilot dry-run outputs (not needed for the paper)
out_dryrun/
out_partial/

# Temporary PDF renders
*.tmp.pdf
